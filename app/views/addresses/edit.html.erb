<h1>Editing address</h1>

<% form_for(@address) do |f| %>
  <%= f.error_messages %>
  
  <%= render :partial => 'form', :object => f %>
  
  <p>
    <%= f.submit 'Save' %> or <%= link_to 'go back', @address %>
  </p>
<% end %>
